/* Import Giaza Font */
@import url('https://fonts.googleapis.com/css2?family=Giaza&display=swap');

/* Reset Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    scroll-behavior: smooth;
}

/* General Styling */
body {
    font-family: Arial, sans-serif;
    background-color: #f8f8f8;
    width: 100vw;
    overflow-x: hidden;
}

/* Header Styling */
header {
    background-color: #ffebf0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 30px;
    position: fixed;
    width: 100%;
    top: 0;
    z-index: 1000;
}

.logo img {
    height: 100px;
    width : auto;
}

nav ul li a img {
    width: 30px;  /* Adjust the width */
    height: 30px; /* Adjust the height */
}

.nav ul li a img:hover {
    background-color: #df7c95; /* Hover color */
    color: white; /* White text on hover */
    transform: scale(1.05); /* Slight size increase */
}

/* Optional: Ensure the nav items are aligned properly */
nav ul {
    display: flex;
    align-items: center;
    gap: 15px; /* Adds spacing between items */
}

nav ul li {
    list-style: none;
}


header {
    height: 80px; /* Keep header height fixed */
    padding: 10px 30px; /* Adjust padding if needed */
    overflow: hidden; /* Prevent layout shift */
    display: flex;
    align-items: center;
}

nav ul {
    list-style: none;
    display: flex;
    align-items: center;
    gap: 20px;
}

nav ul li a {
    text-decoration: none;
    color: black;
    font-weight: bold;
}

nav ul li img {
    height: 20px;
}

/* Hero Section */
.hero {
    position: relative;
    width: 100vw;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
}

.hero-bg {
    position: absolute;
    width: 100%;
    height: 100%;
    background: url('./hpbackground.png') no-repeat center center/cover;
    z-index: -1;
}

.hero-content {
    text-align: center;
    color: white;
}

.learn-more {
    background-color: #ffebf0; /* Light pink background */
    color: #df7c95; /* Primary text color */
    font-size: 14px; /* Smaller size */
    font-weight: bold;
    padding: 8px 16px;
    border: 2px solid #df7c95; /* Border matching the hover color */
    border-radius: 20px; /* Rounded edges */
    cursor: pointer;
    transition: all 0.3s ease-in-out;
}

.learn-more:hover {
    background-color: #df7c95; /* Hover color */
    color: white; /* White text on hover */
    transform: scale(1.05); /* Slight size increase */
}

.learn-more:active {
    background-color: #c9657f; /* Slightly darker shade when clicked */
    transform: scale(0.98); /* Shrink effect on click */
}

.brand-name {
    font-family: 'Giaza', serif;
    font-size: 70px;
    color: #ffebf0;
}

/* Sections */
.about-us, .how-it-works, .additional-features {
    width: 100vw;
    padding: 50px;
    text-align: center;
    background-color: #fdf5e6;
}

.features {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    gap: 20px; /* Space between feature items */
    text-align: center;
}

.feature {
    max-width: 300px; /* Keep features uniform */
    padding: 20px;
    background: #ffebf0; /* Light pink background */
    border-radius: 15px;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Icon Styling */
.feature img {
    width: 50px; /* Adjust icon size */
    height: auto; /* Maintain aspect ratio */
    margin-bottom: 10px; /* Space between icon and title */
    transition: transform 0.3s ease-in-out, opacity 0.3s ease-in-out;
}

/* Button and Icon Alignment */
.feature-btn {
    background-color: #df7c95; /* Primary button color */
    color: white;
    font-size: 14px;
    font-weight: bold;
    padding: 8px 16px;
    border: none;
    border-radius: 20px;
    cursor: pointer;
    transition: all 0.3s ease-in-out;
    margin-top: 10px;
}

.feature-btn:hover {
    background-color: #c9657f;
    transform: scale(1.05);
}

/* Flexbox for buttons and icons */
.feature .actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px; /* Space between button and icon */
    margin-top: 10px;
}

.read-more {
    background-color: #ffebf0; /* Light pink background */
    color: #df7c95; /* Primary text color */
    font-size: 14px; /* Smaller size */
    font-weight: bold;
    padding: 8px 16px;
    border: 2px solid #df7c95; /* Border matching the hover color */
    border-radius: 20px; /* Rounded edges */
    cursor: pointer;
    transition: all 0.3s ease-in-out;
}

.read-more:hover {
    background-color: #df7c95; /* Hover color */
    color: white; /* White text on hover */
    transform: scale(1.05); /* Slight size increase */
}

.read-more:active {
    background-color: #c9657f; /* Slightly darker shade when clicked */
    transform: scale(0.98); /* Shrink effect on click */
}

h2 {
    font-family: 'Giaza', serif;
    font-size: 40px;
    margin-bottom: 20px;
}

.features {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
}

.feature {
    width: 30%;
    padding: 20px;
    background: #f9eadb;
    border-radius: 15px;
    text-align: center;
}

.feature img {
    width: 60px;
    margin-bottom: 15px;
}

h3 {
    font-size: 20px;
    margin-bottom: 10px;
}

p {
    font-size: 16px;
    margin-bottom: 15px;
}

.feature-btn {
    background-color: #c38b6b;
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
}

.feature-btn:hover {
    background-color: #8b5a2b;
}

/* Best Sellers Section */
.best-sellers {
    text-align: center;
    background-color: #fdf5e6;
    padding: 50px 30px;
}

.best-sellers h2 {
    font-family: 'Giaza', serif;
    font-size: 40px;
    color: #8b5a2b;
}

.categories {
    display: flex;
    align-items: center;
    gap: 15px;
}

.categories {
    display: flex;
    align-items: center;
    justify-content: center; /* Centers the categories */
    gap: 15px;
    position: relative; /* Allows absolute positioning for the filter icon */
}

.filter-icon {
    cursor: pointer;
    width: 24px;
    height: 24px;
    position: absolute;
    right: 0; /* Aligns the filter icon to the right */
}

.filter-icon {
    cursor: pointer;
    width: 24px; /* Adjust size as needed */
    height: 24px;
    margin-left: auto;
}

.categories {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin: 20px 0;
    font-weight: bold;
    font-size: 18px;
    color: #553d2a;
}

.categories span {
    cursor: pointer;
    padding-bottom: 5px;
}

.categories .active {
    border-bottom: 3px solid #8b5a2b;
}

.products {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 40px;
}

.product {
    width: 250px;
    background-color: white;
    padding: 15px;
    border-radius: 10px;
    text-align: center;
}

.product img {
    width: 100%;
    border-radius: 10px;
}

.product h3 {
    font-size: 16px;
    margin-top: 10px;
}

.price {
    color: #007bff;
    font-size: 18px;
    font-weight: bold;
    margin-top: 5px;
}

.best-sellers {
    text-align: center;
}

.categories {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 15px;
    position: relative;
}

.filter-icon {
    cursor: pointer;
    width: 24px;
    height: 24px;
    position: absolute;
    right: 0;
}

.products {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
    justify-content: center;
    padding: 20px;
}

.product {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end; /* Ensures text is at the bottom */
    width: 220px; /* Fixed width for uniform size */
    height: 320px; /* Fixed height for uniform size */
    background-color: #f9f9f9; /* Light background */
    padding: 15px;
    border-radius: 10px;
    text-align: center;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    overflow: hidden;
}

.product img {
    width: 100%;
    max-height: 180px; /* Ensures images fit within the box */
    object-fit: contain; /* Keeps aspect ratio */
}

.product h3 {
    font-size: 16px;
    margin: 10px 0 5px;
}

.product .price {
    font-weight: bold;
    color: #3625cf;
    margin-bottom: 5px;
}

/* Footer Styling */
footer {
    background-color: #fde4e4;
    padding: 40px 60px;
    display: flex;
    justify-content: center;
}

.footer-container {
    display: flex;
    justify-content: space-between;
    width: 80%;
}

.footer-column {
    width: 22%;
}

.footer-column h3 {
    font-size: 18px;
    font-weight: bold;
    color: #553d2a;
    text-transform: uppercase;
    margin-bottom: 10px;
    position: relative;
}

.footer-column h3::after {
    content: "";
    display: block;
    width: 40px;
    height: 3px;
    background-color: #c38b6b;
    margin-top: 5px;
}

.footer-column ul {
    list-style: none;
    padding: 0;
}

.footer-column ul li {
    margin-bottom: 10px;
}

.footer-column ul li a {
    text-decoration: none;
    color: #553d2a;
    font-size: 16px;
}

.footer-icons {
    margin-top: 15px;
}

.footer-icons a {
    display: inline-block;
    margin-right: 10px;
}

.footer-icons img {
    width: 30px;
    height: 30px;
    border-radius: 50%;
    background-color: white;
    padding: 5px;
    border: 1px solid #553d2a;
}
